# SPDX-License-Identifier: MIT OR Apache-2.0

name: Release

on:
  push:
    tags:
      - 'v[0-9]+.[0-9]+.[0-9]+*'

permissions:
  contents: write
  id-token: write

env:
  CARGO_TERM_COLOR: always

jobs:
  publish:
    name: Publish to crates.io
    runs-on: ubuntu-24.04
    environment: release
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v6
      - uses: dtolnay/rust-toolchain@stable

      - uses: michaelklishin/rust-build-package-release-action@v2
        with:
          command: validate-version
          validate-cargo-toml: 'true'

      - name: Authenticate with crates.io
        id: auth
        uses: rust-lang/crates-io-auth-action@v1

      # Publish in dependency order.
      # publish-crate treats "already exists" as a no-op, so
      # re-running a partially failed release is safe.

      - name: Publish ldap-client-ber
        uses: michaelklishin/rust-build-package-release-action@v2
        with:
          command: publish-crate
          package: ldap-client-ber
          locked: 'true'
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}

      - name: Publish ldap-client-proto
        uses: michaelklishin/rust-build-package-release-action@v2
        with:
          command: publish-crate
          package: ldap-client-proto
          locked: 'true'
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}

      - name: Publish ldap-client
        uses: michaelklishin/rust-build-package-release-action@v2
        with:
          command: publish-crate
          package: ldap-client
          locked: 'true'
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}

      - name: Publish ldap-client-cli
        uses: michaelklishin/rust-build-package-release-action@v2
        with:
          command: publish-crate
          package: ldap-client-cli
          locked: 'true'
        env:
          CARGO_REGISTRY_TOKEN: ${{ steps.auth.outputs.token }}

  github-release:
    name: Create GitHub Release
    runs-on: ubuntu-24.04
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v6

      - uses: michaelklishin/rust-build-package-release-action@v2
        id: changelog
        with:
          command: extract-changelog

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v2
        with:
          draft: false
          body_path: release_notes.md
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
